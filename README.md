# AgeCalculator_Matlab
In the Matlab programming language, an age calculation program with the help of sql database

There is no need for a database connection for the program to work. But if the connection is not made, all functions will not work.

That's why I recommend that you first make a sql database connection via matlab.

Use the '.sql' extension file in the folder for database installation
